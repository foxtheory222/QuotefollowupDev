# Handoff 2026-04-09

This branch captures the current Quote Follow Up regional repo state after the April 9, 2026 reliability and live-runtime pass, plus the later quote-line and April 9 current-state repair work.

## Remote

- Repo: `https://github.com/foxtheory222/quoteFollowUpV2`
- Working copy: `C:\Users\smcfarlane\Desktop\WorkBench\QuoteFollowUpRegion\tmp-github-quotefollowupv2\quoteFollowUpV2`

## What Is Live

- The Power Pages runtime/web template changes were pushed live in `Applied Canada Operations Hub`.
- The quote detail route was repaired by fixing canonical quote-line handling and deactivating orphan quote headers that had no current line evidence.
- The April 9 captured workbooks were replayed into Dataverse current-state tables with a targeted repair script for `qfu_backorder`, `qfu_budget`, `qfu_branchdailysummary`, and `qfu_ingestionbatch`.
- Confirmed live:
  - `/southern-alberta` no longer shows the duplicate `4171` branch card
  - branch and analytics abnormal margin panels no longer duplicate the current-month billing docs
  - CSSR overdue leaderboard is now distinct-order based instead of raw line-count based
  - quote detail route can render live line rows again for current quotes such as `0515507964`

Primary live verification artifacts:

- `VERIFICATION/live-browser-verification.md`
- `VERIFICATION/live-browser-verification.json`
- `VERIFICATION/route-smoke-checks.md`
- `VERIFICATION/runtime-contract-checks.md`
- `VERIFICATION/live-current-state-from-parsed-workbooks.json`
- `VERIFICATION/live-quote-line-integrity-repair-summary.json`

## What Is Still Broken

- Source freshness is still not truly live.
  - The targeted repair updates ingestion-batch rows to replay-backed evidence, but upstream mailbox-triggered freshness is still not fully repaired/deployed end to end.
  - If new source emails land tomorrow and the live flows drift again, the portal can regress until the flows themselves are fully redeployed.
- Budget correctness is now aligned to the captured April 9 workbook truth in `qfu_budget`, but the live SA1300 flow update path is still partially blocked by the Microsoft flow-server `InternalServerError` issue already documented in the incident notes.
- Backorders were brought back into April 9 current-state alignment from captured workbook truth, but the long-term fix still depends on the live ZBO writer path remaining idempotent and fresh.

## Most Important Current Findings

1. Runtime fixes are deployed live.
2. Quote detail failure was an upstream writer/data defect, not a route bug. Canonical quote-line handling and orphan-header cleanup are now in source and in the live data.
3. April 9 branch actuals and current-state backorders were repaired from captured workbook evidence, not guessed from portal output.
4. Live flow repair is still the remaining gap. The source fixes exist in the generator/scripts, but the SA1300 live patch path is still partially blocked by Microsoft-side workflow update failures.

## Start Here Next

1. Re-audit live quote headers vs `qfu_quoteline` rows after the next real SP830CA arrival.
   - Use or extend:
   - `scripts/check-southern-alberta-live-state.ps1`
   - `scripts/create-southern-alberta-pilot-flow-solution.ps1`
2. Finish live SA1300 flow deployment using a narrower REST patch or create-new-flow path if the generic workflow update path keeps failing.
3. Re-audit the next real SA1300 and ZBO arrivals against the repaired current-state rows to prove the flows, not just the one-time repair, are correct.
4. Keep the parsed-workbook repair scripts available for emergency recovery, but do not treat them as a substitute for live flow repair.
5. Only after the upstream rows are proven correct on a fresh run, rerun:
   - `scripts/invoke-qfu-reliability-diagnostics.ps1`

## Useful Files

- Runtime:
  - `site/web-templates/qfu-regional-runtime/QFU-Regional-Runtime.webtemplate.source.html`
- Flow generator:
  - `RAW/scripts/create-southern-alberta-pilot-flow-solution.ps1`
- Workbook truth + replay helpers:
  - `RAW/scripts/parse-southern-alberta-workbooks.py`
  - `RAW/scripts/deploy-southern-alberta-pilot.ps1`
- Diagnostics:
  - `scripts/invoke-qfu-reliability-diagnostics.ps1`
  - `scripts/run-live-qfu-health-check.ps1`
  - `scripts/verify-live-budget-lineage.ps1`
  - `scripts/audit-live-cssr-overdue-order-counts.ps1`
  - `scripts/audit-live-margin-snapshot-integrity.ps1`
  - `scripts/repair-live-quote-line-integrity.ps1`
  - `scripts/repair-live-current-state-from-parsed-workbooks.ps1`
- Contracts/docs:
  - `CARD_CONTRACTS.md`
  - `CONVENTIONS.md`
  - `CHANGE_MAP.md`
  - `KNOWN_LIMITATIONS.md`

## Notes

- `gh` CLI is not installed in this environment, so publishing was done with plain `git`.
- The operations workspace root is not the durable repo. This nested repo copy is the one intended for continued work and remote access.
